# --- Auth endpoints (READ-ONLY in runtime) ---
LOGIN_URL=            # POST login endpoint
LOGOUT_URL=           # GET logout / invalidate JWT endpoint
PW_CHANGE_URL=        # POST password change endpoint

# --- Task execution endpoints ---
DEFAULT_TASK_URL=     # Default task submission endpoint
REAPPLY_TASK_URL=     # Task endpoint when action == "reapply"

# --- Google Sheets ---
SPREADSHEET_ID=       # Google Spreadsheet ID containing user credentials
root_clientId = 
root_password = 
root_username = 

# --- Auth endpoints (READ-ONLY in runtime) ---
LOGIN_URL=            # POST login endpoint
LOGOUT_URL=           # GET logout / invalidate JWT endpoint
PW_CHANGE_URL=        # POST password change endpoint

# --- Task execution endpoints ---
DEFAULT_TASK_URL=     # Default task submission endpoint
REAPPLY_TASK_URL=     # Task endpoint when action == "reapply"

# --- Google Sheets ---
SPREADSHEET_ID=       # Google Spreadsheet ID containing user credentials
root_clientId = 
root_password = 
root_username = 
default_kitta =
default_price_limit =

applicable_url = 			# To Check for Applicable Issues across global users
issues_url =					# To Check for User Specific Applicable Issues
ownDetail_url =				# To Extract the User's DEMAT
bank_url =						# To Get Bank ID
mybank_url = f				# To Get Bank Account Details
canApply_url = f			# Simple checks for the DEMAT validity
company_url = f				# To Get Share Unit Price and Company Details for log
reapply_details_url = # To get the formID
pin_change_url = /auth/transactionPIN/	# statusCode: 201, status:"CREATED"@confirmTransactionPIN, newTransactionPIN
